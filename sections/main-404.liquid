<section class="wrap">
  <div class="empty">
    <div class="stat__num" style="font-size:4rem">404</div>
    <p class="lead" style="margin:0 auto 24px">This page took a wrong turn in the tree.</p>
    <a href="{{ routes.root_url }}" class="btn btn--primary">Back home</a>
  </div>
</section>

{% schema %}
{ "name": "404" }
{% endschema %}
